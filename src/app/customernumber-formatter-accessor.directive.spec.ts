import { CustomernumberFormatterAccessorDirective } from './customernumber-formatter-accessor.directive';

describe('CustomernumberFormatterAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomernumberFormatterAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
